# compiler-cpp

The embeddable C++ implementation of the GDDL compiler, referenced in
the top-level README and in `HANDOFF.md`'s own longstanding "Known
open questions" as the single oldest deferred item in this project's
history -- predating this restructuring by the project's entire
prior timeline.

**Not started.** Nothing in this directory yet. Placeholder only, so
the top-level layout described in the README matches what actually
exists on disk rather than implying a directory that doesn't.

See `compiler-python/HANDOFF.md`'s "Known open questions" section for
the standing context on why this is deferred and what it would need
to actually begin.
