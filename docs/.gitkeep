Placeholder so this directory survives the restructuring commit --
git doesn't track empty directories. The README references specific
paths under docs/ (export-targets/cpp.md, export-targets/binary-format.md,
export-targets/retro/) that don't exist yet; writing that documentation
content wasn't part of this restructuring task, so this directory is
an empty scaffold matching the new top-level layout, not a claim that
the docs exist.
